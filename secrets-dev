#!/bin/bash
# Simple launcher for Secrets development
# This script can be used from anywhere in the system

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/run-dev.sh" "$@"
